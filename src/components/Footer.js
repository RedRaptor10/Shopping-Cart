import React from "react";

const Footer = ({cart}) => {
	return (
		<div id="footer">
			Shopping Cart created through The Odin Project online course. Coded in React.
		</div>
	);
};

export default Footer;